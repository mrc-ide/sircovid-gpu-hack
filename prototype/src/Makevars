# -*-makefile-*-
OBJECTS = cpp11.o sirs.o sirs_gpu_driver.o sirs_gpu.o
PKG_LIBS = -lcudart

NVCC = nvcc
NVCC_FLAGS = -O2 -G -I. -I$(R_HOME)/include $(DUST_FLAGS) -Xcompiler -fPIC -x cu

%.o: %.cu
	$(NVCC) $(NVCC_FLAGS) -c $< -o $@
